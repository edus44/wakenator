{
  "name": "wakenator-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node .",
    "dev": "NODE_ENV=development DEBUG=wk* node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": " now --public && now alias && (now rm wakenator-server --yes --safe || true)"
  },
  "now": {
    "alias": "wakenator-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "socket.io": "^2.0.3"
  },
  "devDependencies": {
    "now": "^8.3.6",
    "npm-run-all": "^4.1.1"
  }
}
